/* exported last */

function last(array) {
  return array[array.length - 1];
}

// Use the 'return' keyword followed by accessing the last element
// in the array argument that is passed in.
// Do this by subtracting 1 from the length of the array
// and using that value as the index for the array itself.
