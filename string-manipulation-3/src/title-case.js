/* exported titleCase */

function titleCase(title) {
  // var spltStr = title.toLowerCase().split(' ');
  // for (var i = 0; i < spltStr.length; i++) {
  //   for (var j = 0; j < spltStr[i][1]; j++) {
  //     var firstLetter = spltStr[i][j].toUpperCase();
  //   }
  // }
}
