/* exported getLastChar */

function getLastChar(string) {
  return string[string.length - 1];
}

// On the first line of the code block,
// type the 'return' keyword, followed by
// accessing the last character of the
// string argument using bracket notation.
// Subtract 1 from the length property
// of the string; and use this as its index.
