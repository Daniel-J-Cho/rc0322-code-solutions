/* exported getFirstChar */

function getFirstChar(string) {
  return string[0];
}

// In the first line of the code block of the function
// type the 'return' keyword followed by accessing the first
// character of the string argument using bracket notation.
