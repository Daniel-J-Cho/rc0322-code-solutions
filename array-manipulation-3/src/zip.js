/* exported zip */

// Create new array and assign it to a
// variable.
// Loop through 'first' array. Nest another
// for loop for 'second' array. Push
// 'i' of first array and 'j' of second
// array onto [i][j] of new array.
// If either array is longer than the other,
// return.  Return new array.
